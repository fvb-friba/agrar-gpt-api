openapi: 3.1.0
info:
  title: Agrar GPT API – Soil BUEK200 WMS v2
  version: 1.0.5
servers:
  - url: https://agrar-gpt-api.onrender.com
paths:
  /soil:
    get:
      operationId: getSoilData
      summary: Get soil data from BGR BUEK200 WMS
      parameters:
        - in: query
          name: lat
          required: true
          schema:
            type: number
        - in: query
          name: lon
          required: true
          schema:
            type: number
      responses:
        '200':
          description: Soil data
          content:
            application/json:
              schema:
                type: object
